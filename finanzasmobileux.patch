diff --git a/README.md b/README.md
index ebf1cbc..3bb9b76 100644
--- a/README.md
+++ b/README.md
@@ -124,7 +124,7 @@ src/
 └── sw.js                   # Service Worker
 
 dist/
-├── MisFinanzas.html       # Archivo único compilado
+├── index.html             # Archivo único compilado (servido en la raíz)
 ├── sw.js                  # Service Worker
 └── manifest.json          # PWA Manifest
 ```
diff --git a/build.js b/build.js
index 3dd69b9..d93c47b 100644
--- a/build.js
+++ b/build.js
@@ -112,25 +112,24 @@ function buildHTML(css, js) {
   return html;
 }
 
+const APP_ICON_SVG = `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512"><defs><linearGradient id="b" x1="0" y1="0" x2="0" y2="1"><stop offset="0" stop-color="#2a2f45"/><stop offset="1" stop-color="#191a24"/></linearGradient><linearGradient id="g" x1="0" y1="0" x2="1" y2="1"><stop offset="0" stop-color="#7aa2f7"/><stop offset="1" stop-color="#bb9af7"/></linearGradient></defs><rect width="512" height="512" rx="112" fill="url(#b)"/><g transform="translate(56,56) scale(16.6667)" fill="none" stroke="url(#g)" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M12 6V18M9 15.1818L9.87887 15.841C11.0504 16.7197 12.9498 16.7197 14.1214 15.841C15.2929 14.9623 15.2929 13.5377 14.1214 12.659C13.5355 12.2196 12.7677 12 11.9999 12C11.275 12 10.5502 11.7804 9.99709 11.341C8.891 10.4623 8.891 9.03772 9.9971 8.15904C11.1032 7.28036 12.8965 7.28036 14.0026 8.15904L14.4175 8.48863M21 12C21 16.9706 16.9706 21 12 21C7.02944 21 3 16.9706 3 12C3 7.02944 7.02944 3 12 3C16.9706 3 21 7.02944 21 12Z"/></g></svg>`;
+
+const APP_ICON_URI = 'data:image/svg+xml,' + encodeURIComponent(APP_ICON_SVG);
+
 const MANIFEST = {
-  name: 'MisFinanzas',
+  name: 'MisFinanzas · Finanzas personales',
   short_name: 'MisFinanzas',
-  description: 'Control de finanzas personales',
-  start_url: '/finanzas/',
+  description: 'Control de finanzas personales 100% offline: gastos, presupuestos, metas y salud financiera.',
+  start_url: './',
+  scope: './',
   display: 'standalone',
   orientation: 'portrait-primary',
   background_color: '#1e1f2e',
   theme_color: '#1e1f2e',
-  icons: [{
-    src: 'data:image/svg+xml,' + encodeURIComponent(`
-      <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512">
-        <rect width="512" height="512" rx="80" fill="#1e1f2e"/>
-        <text x="256" y="340" text-anchor="middle" font-size="280" font-family="system-ui">💰</text>
-      </svg>`),
-    sizes: '512x512',
-    type: 'image/svg+xml',
-    purpose: 'any maskable'
-  }]
+  icons: [
+    { src: APP_ICON_URI, sizes: '512x512', type: 'image/svg+xml', purpose: 'any' },
+    { src: APP_ICON_URI, sizes: '512x512', type: 'image/svg+xml', purpose: 'maskable' }
+  ]
 };
 
 async function main() {
diff --git a/src/index.html b/src/index.html
index 4bf164a..68cc80a 100644
--- a/src/index.html
+++ b/src/index.html
@@ -2,17 +2,23 @@
 <html lang="es" data-theme="dark">
 <head>
   <meta charset="UTF-8">
-  <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
-  <meta name="description" content="MisFinanzas — control de finanzas personales 100% offline">
-  <title>MisFinanzas</title>
+  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=5.0, viewport-fit=cover">
+  <meta name="description" content="MisFinanzas — control de finanzas personales 100% offline: gastos, presupuestos, metas y salud financiera.">
+  <meta name="color-scheme" content="dark light">
+  <title>MisFinanzas · Finanzas personales offline</title>
 
   <!-- PWA -->
   <link rel="manifest" href="manifest.json">
   <meta name="theme-color" content="#1e1f2e" id="theme-color-meta">
   <meta name="apple-mobile-web-app-capable" content="yes">
+  <meta name="mobile-web-app-capable" content="yes">
   <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
   <meta name="apple-mobile-web-app-title" content="MisFinanzas">
 
+  <!-- Iconos (glifo de moneda en gradiente Tokyo Night) -->
+  <link rel="icon" type="image/svg+xml" href="data:image/svg+xml,%3Csvg%20xmlns='http://www.w3.org/2000/svg'%20viewBox='0%200%20512%20512'%3E%3Cdefs%3E%3ClinearGradient%20id='b'%20x1='0'%20y1='0'%20x2='0'%20y2='1'%3E%3Cstop%20offset='0'%20stop-color='%232a2f45'/%3E%3Cstop%20offset='1'%20stop-color='%23191a24'/%3E%3C/linearGradient%3E%3ClinearGradient%20id='g'%20x1='0'%20y1='0'%20x2='1'%20y2='1'%3E%3Cstop%20offset='0'%20stop-color='%237aa2f7'/%3E%3Cstop%20offset='1'%20stop-color='%23bb9af7'/%3E%3C/linearGradient%3E%3C/defs%3E%3Crect%20width='512'%20height='512'%20rx='112'%20fill='url(%23b)'/%3E%3Cg%20transform='translate(56,56)%20scale(16.6667)'%20fill='none'%20stroke='url(%23g)'%20stroke-width='1.5'%20stroke-linecap='round'%20stroke-linejoin='round'%3E%3Cpath%20d='M12%206V18M9%2015.1818L9.87887%2015.841C11.0504%2016.7197%2012.9498%2016.7197%2014.1214%2015.841C15.2929%2014.9623%2015.2929%2013.5377%2014.1214%2012.659C13.5355%2012.2196%2012.7677%2012%2011.9999%2012C11.275%2012%2010.5502%2011.7804%209.99709%2011.341C8.891%2010.4623%208.891%209.03772%209.9971%208.15904C11.1032%207.28036%2012.8965%207.28036%2014.0026%208.15904L14.4175%208.48863M21%2012C21%2016.9706%2016.9706%2021%2012%2021C7.02944%2021%203%2016.9706%203%2012C3%207.02944%207.02944%203%2012%203C16.9706%203%2021%207.02944%2021%2012Z'/%3E%3C/g%3E%3C/svg%3E">
+  <link rel="apple-touch-icon" href="data:image/svg+xml,%3Csvg%20xmlns='http://www.w3.org/2000/svg'%20viewBox='0%200%20512%20512'%3E%3Cdefs%3E%3ClinearGradient%20id='b'%20x1='0'%20y1='0'%20x2='0'%20y2='1'%3E%3Cstop%20offset='0'%20stop-color='%232a2f45'/%3E%3Cstop%20offset='1'%20stop-color='%23191a24'/%3E%3C/linearGradient%3E%3ClinearGradient%20id='g'%20x1='0'%20y1='0'%20x2='1'%20y2='1'%3E%3Cstop%20offset='0'%20stop-color='%237aa2f7'/%3E%3Cstop%20offset='1'%20stop-color='%23bb9af7'/%3E%3C/linearGradient%3E%3C/defs%3E%3Crect%20width='512'%20height='512'%20rx='112'%20fill='url(%23b)'/%3E%3Cg%20transform='translate(56,56)%20scale(16.6667)'%20fill='none'%20stroke='url(%23g)'%20stroke-width='1.5'%20stroke-linecap='round'%20stroke-linejoin='round'%3E%3Cpath%20d='M12%206V18M9%2015.1818L9.87887%2015.841C11.0504%2016.7197%2012.9498%2016.7197%2014.1214%2015.841C15.2929%2014.9623%2015.2929%2013.5377%2014.1214%2012.659C13.5355%2012.2196%2012.7677%2012%2011.9999%2012C11.275%2012%2010.5502%2011.7804%209.99709%2011.341C8.891%2010.4623%208.891%209.03772%209.9971%208.15904C11.1032%207.28036%2012.8965%207.28036%2014.0026%208.15904L14.4175%208.48863M21%2012C21%2016.9706%2016.9706%2021%2012%2021C7.02944%2021%203%2016.9706%203%2012C3%207.02944%207.02944%203%2012%203C16.9706%203%2021%207.02944%2021%2012Z'/%3E%3C/g%3E%3C/svg%3E">
+
   <!-- CSS (build.js reemplaza esto con <style> inlinado) -->
   <link rel="stylesheet" href="styles/base.css">
   <link rel="stylesheet" href="styles/themes.css">
diff --git a/src/modules/nav.js b/src/modules/nav.js
index 893cc41..f118f60 100644
--- a/src/modules/nav.js
+++ b/src/modules/nav.js
@@ -42,10 +42,37 @@ function init() {
   _initFab();
   _initDrawer();
   _initModal();
+  _initGlobalKeys();
   _checkReminderBanner();
   _applySystemNotificationOnLoad();
 }
 
+// ── Bloqueo de scroll de fondo (modal / drawer) ─────────────────────────────
+
+function _lockScroll() {
+  document.body.classList.add('scroll-locked');
+}
+
+// Solo se libera cuando ni el modal ni el drawer siguen abiertos.
+function _maybeUnlockScroll() {
+  const modalOpen  = document.getElementById('modal')?.classList.contains('active');
+  const drawerOpen = document.getElementById('drawer')?.classList.contains('active');
+  if (!modalOpen && !drawerOpen) document.body.classList.remove('scroll-locked');
+}
+
+// ── Teclas globales (Escape cierra capas superpuestas) ──────────────────────
+
+function _initGlobalKeys() {
+  document.addEventListener('keydown', e => {
+    if (e.key !== 'Escape') return;
+    if (document.getElementById('modal')?.classList.contains('active')) {
+      closeModal();
+    } else if (document.getElementById('drawer')?.classList.contains('active')) {
+      closeDrawer();
+    }
+  });
+}
+
 // ── Router (hash-based) ────────────────────────────────────────────────────
 
 function _initRouter() {
@@ -179,13 +206,13 @@ function _initDrawer() {
 function openDrawer() {
   document.getElementById('drawer')?.classList.add('active');
   document.getElementById('drawer-overlay')?.classList.add('active');
-  document.body.style.overflow = 'hidden';
+  _lockScroll();
 }
 
 function closeDrawer() {
   document.getElementById('drawer')?.classList.remove('active');
   document.getElementById('drawer-overlay')?.classList.remove('active');
-  document.body.style.overflow = '';
+  _maybeUnlockScroll();
 }
 
 // ── Tema ───────────────────────────────────────────────────────────────────
@@ -279,6 +306,8 @@ function showModal(trustedHTML, title, buttons) {
 
   document.getElementById('modal-overlay')?.classList.add('active');
   modal.classList.add('active');
+  modal.scrollTop = 0;
+  _lockScroll();
 
   setTimeout(() => modal.querySelector('input, select, textarea')?.focus(), 50);
 }
@@ -286,6 +315,7 @@ function showModal(trustedHTML, title, buttons) {
 function closeModal() {
   document.getElementById('modal-overlay')?.classList.remove('active');
   document.getElementById('modal')?.classList.remove('active');
+  _maybeUnlockScroll();
 }
 
 // ── Toast ──────────────────────────────────────────────────────────────────
diff --git a/src/styles/base.css b/src/styles/base.css
index 96e9cfb..16a1b6c 100644
--- a/src/styles/base.css
+++ b/src/styles/base.css
@@ -40,6 +40,12 @@
   --shadow:   0 4px 16px rgba(0, 0, 0, 0.35);
   --shadow-sm: 0 2px 8px rgba(0, 0, 0, 0.25);
   --transition: 0.2s ease;
+
+  /* Safe areas (notch / home indicator / landscape) — 0 en equipos sin recorte */
+  --safe-top:    env(safe-area-inset-top, 0px);
+  --safe-bottom: env(safe-area-inset-bottom, 0px);
+  --safe-left:   env(safe-area-inset-left, 0px);
+  --safe-right:  env(safe-area-inset-right, 0px);
 }
 
 /* ── Responsive layout tokens (Tailwind-scale) ───────── */
@@ -79,6 +85,8 @@
 html, body {
   height: 100%;
   overflow: hidden;
+  /* Evita el "rebote"/pull-to-refresh que descoloca el layout fijo en móvil */
+  overscroll-behavior: none;
 }
 
 body {
@@ -88,8 +96,14 @@ body {
   color: var(--text);
   background: var(--bg);
   -webkit-font-smoothing: antialiased;
+  -webkit-tap-highlight-color: transparent;
+  text-size-adjust: 100%;
+  -webkit-text-size-adjust: 100%;
 }
 
+/* Bloqueo de scroll de fondo cuando hay un modal/drawer abierto */
+body.scroll-locked .main-content { overflow: hidden; }
+
 /* ── Tipografía ──────────────────────────────────────── */
 h1, h2, h3, h4 { font-weight: 600; color: var(--text); }
 h1 { font-size: 22px; }
diff --git a/src/styles/components.css b/src/styles/components.css
index 0497a59..87f1b28 100644
--- a/src/styles/components.css
+++ b/src/styles/components.css
@@ -137,9 +137,12 @@
   border-radius: var(--radius);
   box-shadow: var(--shadow);
   z-index: 300;
-  width: min(480px, calc(100vw - 32px));
-  max-height: calc(100vh - 64px);
+  width: min(480px, calc(100vw - 32px - var(--safe-left) - var(--safe-right)));
+  /* dvh + safe-areas: el modal nunca queda bajo el notch ni el home indicator */
+  max-height: calc(100dvh - 32px - var(--safe-top) - var(--safe-bottom));
   overflow-y: auto;
+  -webkit-overflow-scrolling: touch;
+  overscroll-behavior: contain;
 }
 
 .modal.active { display: flex; flex-direction: column; }
@@ -183,6 +186,8 @@
 .form-select,
 .form-textarea {
   width: 100%;
+  min-width: 0;          /* impide que inputs desborden celdas de grid (form-row) */
+  max-width: 100%;
   padding: 9px 12px;
   background: var(--bg3);
   border: 1px solid var(--border);
@@ -194,6 +199,14 @@
   transition: border-color var(--transition);
 }
 
+/* En móvil los inputs deben tener ≥16px para que iOS no haga zoom al enfocar */
+@media (max-width: 767px) {
+  .form-input,
+  .form-select,
+  .form-textarea,
+  .search-bar input { font-size: 16px; }
+}
+
 .form-input:focus,
 .form-select:focus,
 .form-textarea:focus {
@@ -207,10 +220,16 @@
 
 .form-row {
   display: grid;
-  grid-template-columns: 1fr 1fr;
+  /* minmax(0,1fr) evita que el contenido ancho (fechas, montos) desborde */
+  grid-template-columns: repeat(2, minmax(0, 1fr));
   gap: 12px;
 }
 
+/* En pantallas muy angostas, apila los pares para que quepan sin recortarse */
+@media (max-width: 360px) {
+  .form-row { grid-template-columns: 1fr; }
+}
+
 /* ── Pills / Tags ────────────────────────────────────── */
 .pill {
   display: inline-flex;
@@ -250,8 +269,10 @@
 /* ── Toast notifications ─────────────────────────────── */
 .toast-container {
   position: fixed;
-  top: calc(var(--topbar-height) + 8px);
-  right: 16px;
+  top: calc(var(--topbar-height) + var(--safe-top) + 8px);
+  right: calc(16px + var(--safe-right));
+  left: calc(16px + var(--safe-left));
+  align-items: flex-end;
   z-index: 400;
   display: flex;
   flex-direction: column;
diff --git a/src/styles/layout.css b/src/styles/layout.css
index 555e029..311a9cc 100644
--- a/src/styles/layout.css
+++ b/src/styles/layout.css
@@ -2,25 +2,30 @@
 .topbar {
   position: fixed;
   top: 0; left: 0; right: 0;
-  height: var(--topbar-height);
+  /* La altura crece con la safe-area superior (notch / status bar traslúcida) */
+  height: calc(var(--topbar-height) + var(--safe-top));
   background: var(--bg2);
   border-bottom: 1px solid var(--border);
   display: flex;
   align-items: center;
   justify-content: space-between;
-  padding: 0 16px;
+  padding: var(--safe-top) calc(16px + var(--safe-right)) 0 calc(16px + var(--safe-left));
   z-index: 100;
   gap: 8px;
 }
 
-/* lg */ @media (min-width: 1024px) { .topbar { padding: 0 28px; } }
-/* xl */ @media (min-width: 1280px) { .topbar { padding: 0 40px; } }
-/* 2xl*/ @media (min-width: 1536px) { .topbar { padding: 0 56px; } }
+/* lg */ @media (min-width: 1024px) { .topbar { padding: var(--safe-top) calc(28px + var(--safe-right)) 0 calc(28px + var(--safe-left)); } }
+/* xl */ @media (min-width: 1280px) { .topbar { padding: var(--safe-top) calc(40px + var(--safe-right)) 0 calc(40px + var(--safe-left)); } }
+/* 2xl*/ @media (min-width: 1536px) { .topbar { padding: var(--safe-top) calc(56px + var(--safe-right)) 0 calc(56px + var(--safe-left)); } }
 
 .topbar-title {
   font-size: 16px;
   font-weight: 600;
   flex: 1;
+  min-width: 0;
+  white-space: nowrap;
+  overflow: hidden;
+  text-overflow: ellipsis;
 }
 
 /* lg */ @media (min-width: 1024px) { .topbar-title { font-size: 17px; } }
@@ -44,15 +49,16 @@
     display: flex;
     flex-direction: column;
     position: fixed;
-    top: var(--topbar-height);
+    top: calc(var(--topbar-height) + var(--safe-top));
     left: 0;
     bottom: 0;
-    width: var(--sidebar-width);
+    width: calc(var(--sidebar-width) + var(--safe-left));
     background: var(--bg2);
     border-right: 1px solid var(--border);
     overflow-y: auto;
     z-index: 90;
-    padding: 8px 0;
+    padding: 8px 0 calc(8px + var(--safe-bottom));
+    padding-left: var(--safe-left);
   }
 }
 
@@ -131,20 +137,21 @@
 /* ── Main content area ───────────────────────────────── */
 .main-content {
   position: fixed;
-  top: var(--topbar-height);
+  top: calc(var(--topbar-height) + var(--safe-top));
   left: 0;
   right: 0;
-  bottom: var(--bottom-tabs-height);
+  bottom: calc(var(--bottom-tabs-height) + var(--safe-bottom));
   overflow-y: auto;
-  padding: 16px;
+  -webkit-overflow-scrolling: touch;
+  padding: 16px calc(16px + var(--safe-right)) 16px calc(16px + var(--safe-left));
   scroll-behavior: smooth;
 }
 
-/* sm  — 640px  */ @media (min-width:  640px) { .main-content { padding: 20px;        } }
-/* md  — 768px  */ @media (min-width:  768px) { .main-content { left: var(--sidebar-width); bottom: 0; padding: 20px 24px; } }
-/* lg  — 1024px */ @media (min-width: 1024px) { .main-content { padding: 24px 32px;    } }
-/* xl  — 1280px */ @media (min-width: 1280px) { .main-content { padding: 28px 40px;    } }
-/* 2xl — 1536px */ @media (min-width: 1536px) { .main-content { padding: 32px 56px;    } }
+/* sm  — 640px  */ @media (min-width:  640px) { .main-content { padding: 20px calc(20px + var(--safe-right)) 20px calc(20px + var(--safe-left)); } }
+/* md  — 768px  */ @media (min-width:  768px) { .main-content { left: calc(var(--sidebar-width) + var(--safe-left)); bottom: 0; padding: 20px calc(24px + var(--safe-right)) calc(20px + var(--safe-bottom)) 24px; } }
+/* lg  — 1024px */ @media (min-width: 1024px) { .main-content { padding: 24px calc(32px + var(--safe-right)) calc(24px + var(--safe-bottom)) 32px; } }
+/* xl  — 1280px */ @media (min-width: 1280px) { .main-content { padding: 28px calc(40px + var(--safe-right)) calc(28px + var(--safe-bottom)) 40px; } }
+/* 2xl — 1536px */ @media (min-width: 1536px) { .main-content { padding: 32px calc(56px + var(--safe-right)) calc(32px + var(--safe-bottom)) 56px; } }
 
 /* ── Views ───────────────────────────────────────────── */
 .view { display: none; }
@@ -163,7 +170,11 @@
 .bottom-tabs {
   position: fixed;
   bottom: 0; left: 0; right: 0;
-  height: var(--bottom-tabs-height);
+  /* Altura + relleno para no quedar debajo del home indicator */
+  height: calc(var(--bottom-tabs-height) + var(--safe-bottom));
+  padding-bottom: var(--safe-bottom);
+  padding-left: var(--safe-left);
+  padding-right: var(--safe-right);
   background: var(--bg2);
   border-top: 1px solid var(--border);
   display: flex;
@@ -197,8 +208,8 @@
 /* ── FAB (mobile) ────────────────────────────────────── */
 .fab {
   position: fixed;
-  bottom: calc(var(--bottom-tabs-height) + 16px);
-  right: 16px;
+  bottom: calc(var(--bottom-tabs-height) + var(--safe-bottom) + 16px);
+  right: calc(16px + var(--safe-right));
   width: var(--fab-size);
   height: var(--fab-size);
   border-radius: 50%;
@@ -247,12 +258,13 @@
   background: var(--bg2);
   border-top: 1px solid var(--border);
   border-radius: 16px 16px 0 0;
-  padding: 8px 8px calc(8px + env(safe-area-inset-bottom));
+  padding: 8px calc(8px + var(--safe-right)) calc(8px + var(--safe-bottom)) calc(8px + var(--safe-left));
   z-index: 200;
   transform: translateY(100%);
   transition: transform 0.3s cubic-bezier(0.32, 0.72, 0, 1);
-  max-height: 80vh;
+  max-height: min(80vh, 80dvh);
   overflow-y: auto;
+  -webkit-overflow-scrolling: touch;
 }
 
 .drawer.active { transform: translateY(0); }
diff --git a/src/sw.js b/src/sw.js
index da8c05e..d0fa8d7 100644
--- a/src/sw.js
+++ b/src/sw.js
@@ -1,9 +1,10 @@
 // src/sw.js — MisFinanzas v2 Service Worker
 // Estrategia: Cache-First con actualización en background.
 
-const CACHE_NAME = 'misfinanzas-v2';
+const CACHE_NAME = 'misfinanzas-v3';
 const PRECACHE   = [
-  './MisFinanzas.html',
+  './',
+  './index.html',
   './manifest.json'
 ];
 
@@ -67,7 +68,7 @@ self.addEventListener('notificationclick', event => {
         for (const client of clientList) {
           if ('focus' in client) return client.focus();
         }
-        return clients.openWindow('./MisFinanzas.html');
+        return clients.openWindow('./');
       })
   );
 });
