{
  "name": "misfinanzas",
  "version": "2.0.0",
  "description": "Control de finanzas personales — 100% offline PWA",
  "scripts": {
    "build": "node build.js",
    "test": "node --test test/*.test.js",
    "dev": "node build.js && open dist/MisFinanzas.html"
  },
  "engines": { "node": ">=18" },
  "devDependencies": {}
}
